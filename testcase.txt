// case
// origin is (350, 200);	-- set origin
// rot is pi/6;			-- set rot
// scale is (2, 1);		-- set scale
for t from -100 to 100 step 1 draw (t, 0);	-- set x-axis
for t from -100 to 100 step 1 draw (0, t);	-- set y-axis
for t from 0 to 2*pi step pi/50 draw (cos(t),sin(t));
